#include "List.h"

template<class T>
void List<T> :: SetFirstElement(T value)
{
	/*Node* newnode = new Node;
	newnode->data = data;
	newnode->next = nullptr;
    this->head = newnode;
    this->size++;*/
}
